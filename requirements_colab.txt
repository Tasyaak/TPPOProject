# Конфиги/утилиты
python-dotenv
PyYAML
packaging
tqdm
rich
regex

# Данные / IO
pandas
pyarrow

# Классический ML
scikit-learn
scipy
joblib
threadpoolctl

# Визуализация (если нужно в ноутбуке)
matplotlib
seaborn
pillow

# NLP / морфология (русский)
nltk
pymorphy2
pymorphy2-dicts-ru
DAWG-Python

# Deep Learning (опционально)
# Если вы используете "import keras" (standalone Keras 3), раскомментируйте:
# keras>=3.0,<4
# h5py  # полезно для сохранения моделей/весов в HDF5, если используете

# Примечание:
# TensorFlow / PyTorch / JAX в Colab обычно уже установлены.
# Ставить их через pip стоит только если вы осознанно меняете backend/версию.