Для запуска обучения нужна писать:
```
python train.py --config configs/default.yaml
```

Перед запуском `db/scripts/generate_data.py` в `Developer Command Prompt` написать:
```
chcp 65001
```

Конкатенация кода ошибки с описанием ошибки в следующем виде:
```
код_ошибки: описание_ошибки
```
